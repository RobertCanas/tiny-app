<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <ul>
    <% for (var key in urls) { %>
      <li><%= key + " : " + urls[key] %></li>
    <% }; %>
  </ul>
</body>
</html>