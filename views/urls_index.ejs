<!DOCTYPE html>
<html lang="en">
<head>

</head>
<body>
  <% include partials/_header %>
  <ul>

    <% //Object.keys(urls).forEach((key) => { %>
      <%= /*key + " : " + urls[key] */%>
    <% //});%>

    <% for (var key in urls) { %>
      <li><%= key + " : " + urls[key] %></li>
    <% }; %>
  </ul>
  <li><a href="#">Shorten Link!</a></li>
</body>
</html>